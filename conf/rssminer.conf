script
if [ ! -d "/var/log/rssminer" ]; then
    mkdir -p "/var/log/rssminer"
fi
if [ ! -d "/var/rssminer" ]; then
    mkdir -p "/var/rssminer"
fi

/var/www/rssminer/scripts/run \
    --profile prod \
    --port 8100 \
    --db-path /var/rssminer/rssminer \
    --h2-trace false \
    --auto-server false \
    --run-crawler false \
    --index-path /var/rssminer/index \
    >> /var/log/rssminer/server.log 2>&1
end script
